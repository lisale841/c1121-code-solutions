/* exported head */
/* - create a storage for the first item in the list.
   -  update first item storage with the first item from the list.
   -  give back the first item.
   */

function head(array) {
  var element = array[0];
  return element;
}
