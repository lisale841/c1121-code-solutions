/*  -create a storage to hold the value.
    - find the value and store it into the storage
    - give back the value.
*/
/* exported getValue */
function getValue(object, key) {
  var value = object[key];
  return value;
}
